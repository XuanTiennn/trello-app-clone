import React from 'react';
import PropTypes from 'prop-types';
import './column.scss'
Column.propTypes = {
    
};

function Column(props) {
    return (
        <div>
            Column
        </div>
    );
}

export default Column;
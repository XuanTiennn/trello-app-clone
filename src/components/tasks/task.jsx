import React from 'react';
import PropTypes from 'prop-types';

Task.propTypes = {
    
};

function Task(props) {
    return (
        <div>
            Task
        </div>
    );
}

export default Task;